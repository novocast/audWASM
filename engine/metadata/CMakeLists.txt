# M15 metadata parsers — see documentation/tasks/M15-metadata.md.
#
# Like engine/analysis/*, this folder has no dedicated target: its sources land directly in
# aud_core's list in the parent CMakeLists.txt (see the "metadata/*.cpp" block there). Kept as a
# subdirectory (rather than a bare folder) purely so it has build-system presence, matching the
# convention set by engine/analysis/CMakeLists.txt.
