# Analyzer interface + registry, stats, silence, clipping, dc, loudness. See M08-M14, M20.
# analyzer.hpp (the Analyzer interface, M00 §6) is header-only and needs no source entry.
# No other sources yet — the registry and concrete analysers land with M08-M14/M20.
